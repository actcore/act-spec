openapi: 3.2.0
info:
  title: ACT HTTP API
  version: 0.2.0
  description: |
    A stateless HTTP API for discovering and invoking tools.
    Any HTTP server that conforms to this specification is a valid implementation.

    Metadata is always passed in the request body — there are no metadata-related headers.
    Every endpoint that accepts metadata supports both `POST` and `QUERY` with identical
    request/response formats. `QUERY` is safe and cacheable; `POST` is the universal fallback.

    See ACT-HTTP.md for the full prose specification.
    See ACT-CONSTANTS.md for the authoritative list of well-known constants.
  license:
    name: Apache-2.0

paths:
  /info:
    get:
      operationId: getInfo
      summary: Component info
      description: Returns component metadata in the same format as the `act:component` WASM custom section.
      responses:
        "200":
          description: Component info.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComponentInfo"

  /metadata-schema:
    post:
      operationId: getMetadataSchema
      summary: Metadata schema
      description: |
        Returns a JSON Schema describing the metadata this server accepts.
        Returns 204 if the server does not require metadata.
        Supports iterative discovery — include current metadata in the body
        to get an expanded schema for bridge components.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Metadata JSON Schema.
          content:
            application/json:
              schema:
                type: object
                description: A valid JSON Schema object of type "object".
        "204":
          description: Server does not require metadata.
    query:
      operationId: queryMetadataSchema
      summary: Metadata schema (safe, cacheable)
      description: Identical to `POST /metadata-schema` but uses the `QUERY` method.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Metadata JSON Schema.
          content:
            application/json:
              schema:
                type: object
        "204":
          description: Server does not require metadata.

  /tools:
    post:
      operationId: listTools
      summary: List tools
      description: Returns the list of available tools. Metadata is passed in the request body.
      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Tool list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListToolsResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"
    query:
      operationId: queryTools
      summary: List tools (safe, cacheable)
      description: |
        Identical to `POST /tools` but uses the `QUERY` method
        (draft-ietf-httpbis-safe-method-w-body), which is safe and idempotent.
      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Tool list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListToolsResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /tools/{name}:
    post:
      operationId: callTool
      summary: Invoke a tool
      description: |
        Invokes a tool and returns the result.

        The response format depends on the `Accept` header:
        - `application/json` (default) — the server collects all results and returns a single JSON response.
        - `text/event-stream` — the server streams results as Server-Sent Events (SSE).

        The server MUST NOT stream SSE unless the client explicitly requests it via `Accept: text/event-stream`.
      parameters:
        - name: name
          in: path
          required: true
          description: Tool name as returned by list-tools.
          schema:
            type: string
        - $ref: "#/components/parameters/AcceptLanguage"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToolCallRequest"
      responses:
        "200":
          description: |
            Tool result. Content-Type is `application/json` for non-streaming
            or `text/event-stream` for streaming responses.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToolCallResponse"
            text/event-stream:
              schema:
                type: string
                description: |
                  SSE stream with the following event types:
                  - `event: content` — a content part (data is a ContentPart JSON object).
                  - `event: error` — a terminal error (data is a ToolError JSON object).
                  - `event: done` — stream complete (data is `{}`).
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/InvalidArgs"
        "500":
          $ref: "#/components/responses/InternalError"
        "504":
          $ref: "#/components/responses/Timeout"
    query:
      operationId: queryTool
      summary: Invoke a read-only, idempotent tool (safe, cacheable)
      description: |
        Invokes a tool using the `QUERY` method. Only available for tools that declare
        both `std:read-only: true` and `std:idempotent: true` in their metadata.

        The server MUST return `405 Method Not Allowed` for tools that are not both
        read-only and idempotent.

        Request and response formats are identical to `POST /tools/{name}`.
      parameters:
        - name: name
          in: path
          required: true
          description: Tool name as returned by list-tools.
          schema:
            type: string
        - $ref: "#/components/parameters/AcceptLanguage"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToolCallRequest"
      responses:
        "200":
          description: |
            Tool result. Content-Type is `application/json` for non-streaming
            or `text/event-stream` for streaming responses.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToolCallResponse"
            text/event-stream:
              schema:
                type: string
                description: |
                  SSE stream with the following event types:
                  - `event: content` — a content part (data is a ContentPart JSON object).
                  - `event: error` — a terminal error (data is a ToolError JSON object).
                  - `event: done` — stream complete (data is `{}`).
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "405":
          description: Tool is not both read-only and idempotent.
        "422":
          $ref: "#/components/responses/InvalidArgs"
        "500":
          $ref: "#/components/responses/InternalError"
        "504":
          $ref: "#/components/responses/Timeout"

  /events:
    post:
      operationId: subscribeEvents
      summary: Subscribe to server events
      description: |
        Opens a Server-Sent Events stream for push notifications.
        Metadata is passed in the request body.
        The stream stays open until the client disconnects.
        Servers that do not support events return 404.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: SSE event stream.
          content:
            text/event-stream:
              schema:
                type: string
                description: |
                  SSE stream with event kinds as event types:
                  - `event: std:tools:changed` — tool list changed
                  - `event: std:resources:changed` — resource list changed
                  - `event: std:events:changed` — event type list changed
                  - `event: <custom>` — custom event kind
                  Data is a JSON object (or `{}` for events with no payload).
        "404":
          $ref: "#/components/responses/NotFound"
    query:
      operationId: queryEvents
      summary: Subscribe to server events (safe)
      description: Identical to `POST /events` but uses the `QUERY` method.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: SSE event stream.
          content:
            text/event-stream:
              schema:
                type: string
        "404":
          $ref: "#/components/responses/NotFound"

  /resources:
    post:
      operationId: listResources
      summary: List available resources
      description: Returns the list of resources available from this component. Metadata is passed in the request body.
      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Resource list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResourcesResponse"
        "500":
          $ref: "#/components/responses/InternalError"
    query:
      operationId: queryResources
      summary: List available resources (safe, cacheable)
      description: Identical to `POST /resources` but uses the `QUERY` method.
      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Resource list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResourcesResponse"
        "500":
          $ref: "#/components/responses/InternalError"

  /resources/{uri}:
    post:
      operationId: getResource
      summary: Get a resource
      description: Returns a resource as raw bytes with the appropriate Content-Type header. Metadata is passed in the request body.
      parameters:
        - name: uri
          in: path
          required: true
          description: Resource URI as returned by list-resources.
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Resource content.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    query:
      operationId: queryResource
      summary: Get a resource (safe, cacheable)
      description: Identical to `POST /resources/{uri}` but uses the `QUERY` method.
      parameters:
        - name: uri
          in: path
          required: true
          description: Resource URI as returned by list-resources.
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataRequest"
      responses:
        "200":
          description: Resource content.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

components:
  parameters:
    AcceptLanguage:
      name: Accept-Language
      in: header
      required: false
      description: Preferred language for localized strings (BCP 47).
      schema:
        type: string
        example: en

    ActProtocolVersion:
      name: ACT-Protocol-Version
      in: header
      required: false
      description: |
        ACT-HTTP protocol version (SemVer major.minor).
        Client sends its maximum supported version; server responds with the effective version.
        Incompatible versions result in 406 Not Acceptable.
      schema:
        type: string
        example: "0.2"

  schemas:
    ComponentInfo:
      type: object
      description: |
        Component metadata. Same format as the `act:component` WASM custom section.
        Uses namespaced tables; well-known metadata lives in the `std` table.
        Additional namespaces MAY be present and MUST be ignored if unrecognized.
      properties:
        std:
          type: object
          description: Standard component metadata.
          properties:
            name:
              type: string
              description: Component name.
              example: weather-tools
            version:
              type: string
              description: SemVer version.
              example: "1.2.0"
            description:
              type: string
              description: Human-readable description.
              example: Weather data tools
            default-language:
              type: string
              description: BCP 47 language tag for the component's default language.
              example: en
            capabilities:
              type: array
              items:
                $ref: "#/components/schemas/Capability"
          required:
            - name
            - version
      additionalProperties: true

    Capability:
      type: object
      required:
        - id
        - required
      properties:
        id:
          type: string
          description: Capability identifier (namespaced URI).
          example: wasi:http/outgoing-handler
        required:
          type: boolean
          description: Whether the component cannot function without this capability.
        description:
          type: string
          description: Human-readable description.

    ToolDefinition:
      type: object
      required:
        - name
        - description
        - parameters_schema
      properties:
        name:
          type: string
          description: Unique tool identifier.
          example: get_weather
        description:
          type: string
          description: Human-readable description (resolved to requested language).
          example: Get current weather for a location
        parameters_schema:
          type: object
          description: JSON Schema with `type: "object"` describing the tool's parameters.
        metadata:
          $ref: "#/components/schemas/Metadata"

    ListToolsResponse:
      type: object
      required:
        - tools
      properties:
        tools:
          type: array
          items:
            $ref: "#/components/schemas/ToolDefinition"
        metadata:
          $ref: "#/components/schemas/Metadata"

    ResourceInfo:
      type: object
      required:
        - uri
        - description
      properties:
        uri:
          type: string
          description: Resource identifier.
          example: "std:icon"
        mime_type:
          type:
            - string
            - "null"
          description: Expected MIME type.
          example: image/png
        description:
          type: string
          description: Human-readable description (resolved to requested language).
          example: Component icon
        metadata:
          $ref: "#/components/schemas/Metadata"

    ListResourcesResponse:
      type: object
      required:
        - resources
      properties:
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ResourceInfo"
        metadata:
          $ref: "#/components/schemas/Metadata"

    MetadataRequest:
      type: object
      properties:
        metadata:
          type: object
          description: Metadata matching the metadata schema.

    ToolCallRequest:
      type: object
      required:
        - arguments
      properties:
        arguments:
          type: object
          description: Tool arguments conforming to the tool's `parameters_schema`.
        metadata:
          type: object
          description: Optional metadata matching the metadata schema.

    ToolCallResponse:
      type: object
      required:
        - content
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/ContentPart"
        metadata:
          $ref: "#/components/schemas/Metadata"

    ContentPart:
      type: object
      required:
        - data
      properties:
        data:
          type: string
          description: |
            Payload. Plain text for `text/*` MIME types, base64-encoded for binary types.
          example: "London: 15°C, partly cloudy"
        mime_type:
          type:
            - string
            - "null"
          description: MIME type. Defaults to `application/json` if absent.
          example: text/plain
        metadata:
          $ref: "#/components/schemas/Metadata"

    ToolError:
      type: object
      required:
        - kind
        - message
      properties:
        kind:
          type: string
          description: |
            Error category. Well-known values use the `std:` prefix.
            See ACT-CONSTANTS.md Section 8 for the complete list.
          example: std:invalid-args
        message:
          type: string
          description: Human-readable error message (resolved to requested language).
          example: "Parameter 'limit' must be a positive integer"
        metadata:
          $ref: "#/components/schemas/Metadata"

    ErrorResponse:
      type: object
      required:
        - error
      properties:
        error:
          $ref: "#/components/schemas/ToolError"

    Metadata:
      type: object
      additionalProperties: true
      description: |
        Key-value metadata. Well-known keys use the `std:` prefix.
        See ACT-CONSTANTS.md for the complete list.

  responses:
    NotFound:
      description: Tool not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error:
              kind: "std:not-found"
              message: "Tool 'foo' does not exist"

    InvalidArgs:
      description: Arguments or metadata do not match schema.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error:
              kind: "std:invalid-args"
              message: "Parameter 'limit' must be a positive integer"

    Forbidden:
      description: Required capability not available.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error:
              kind: "std:capability-denied"
              message: "Network access not permitted"

    Timeout:
      description: Operation timed out.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error:
              kind: "std:timeout"
              message: "Operation exceeded 30s timeout"

    InternalError:
      description: Unexpected server error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error:
              kind: "std:internal"
              message: "Internal server error"
